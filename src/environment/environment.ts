export const environment = {
    production: false,
    endpoint: "http://localhost:5287/api/",
    idFrai: 1, //buscar en bd en la tabla Area el id (AreaID) del registro del FRAI
    idMvp: 2, //buscar en bd en la tabla Area el id (AreaID) del registro del FRAI
    idResponsableClasificacion: 3, //buscar en bd en la tabla Area el id (AreaID) del registro del FRAI
  };